<%= form_with(model: succulent) do |form| %>
  <% if succulent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(succulent.errors.count, "error") %> prohibited this succulent from being saved:</h2>

      <ul>
        <% succulent.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
