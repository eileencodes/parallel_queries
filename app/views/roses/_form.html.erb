<%= form_with(model: rose) do |form| %>
  <% if rose.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rose.errors.count, "error") %> prohibited this rose from being saved:</h2>

      <ul>
        <% rose.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
